# CC Monitor Prompt — P15-v2 Provisioning Validation (post-savini deletion)

**Context for Dean:** Paste the block below into Claude Code **after** you click Provision on P15-v2 (NS = `lavine.info`) in the dashboard UI. Single self-contained block. New session, on `main`, effort = ultrathink, full permissions. This is the first real hands-free validation of the SOA-serial fix chain shipped in PR #18 (`7b64ad3`) running on a CLEAN domain slate — the earlier P15 attempt (NS = camire.info, job `13bb6949`) halted at Step 1 create_vps due to a Linode service-count cap. Dean deleted both P14 savini.info Linodes (45.56.75.67 + 45.79.213.21) to free 2 slots and re-picked domains from a fresh 22-domain Namecheap batch. P14's `server_pairs` row + historical ssh_credentials rows are now orphans pointing at deleted infrastructure — leave them alone.

---

## PASTE BLOCK — P15-v2 MONITOR (copy everything between the fences)

```
You are monitoring the P15-v2 hands-free provisioning run on `main`. The SOA-serial fix chain shipped in PR #18 (commit 7b64ad3) — patchDomainSHSOATemplate at Step 2, un-swallowed v-change-dns-domain-soa at hestia-scripts.ts:285-290, tightened Check 5 (validateSOASerialFormat enforcing yyyymmdd <= today_UTC), and fix_soa_serial_format auto-fix in auto-fix.ts. The first P15 attempt earlier today (2026-04-22, job 13bb6949-51fb-4fab-aa6a-e53962712dad, NS camire.info) halted at Step 1 create_vps because Linode returned HTTP 400 "limit for the number of active services". Dean then DELETED both P14 (savini.info) Linodes — S1=45.56.75.67 and S2=45.79.213.21 — to free two slots, and re-clicked Provision with a fresh 11-domain slate: NS=lavine.info, sending=[carosi, cirone, lodema, luvena, marife, morice, norita, renita, valone, verina] (all .info, all zero-Wayback). P14's server_pairs row (id=fbc03039-00db-4b93-b996-da16c1345814) + ssh_credentials rows for historical savini IPs 173.230.132.245 and 45.33.63.216 are now orphans pointing at deleted infrastructure. DO NOT delete them, DO NOT "recover" P14 — they are deliberately left alone for a later cleanup session. P15-v2 must prove the SOA fix chain produces 0-error zones end-to-end with zero human touch.

Enable ultrathink. Full autonomous permissions. Do NOT wait for me between phases. Do NOT ask questions. Work the whole monitor loop through to a verdict, then write the report, then update memory in Phase 5, then commit.

=== PHASE 0: ORIENT ===

1. `cd /Users/deanhofer/Documents/Claude/Projects/Master Claude Cowork/dashboard-app`
2. `git status && git rev-parse HEAD` — confirm you're on main at 7b64ad3 or later.
3. Read these three files before anything else (do not skip):
   - `src/lib/provisioning/pair-provisioning-saga.ts` (saga entrypoint + step list)
   - `src/lib/provisioning/verification-checks.ts` lines 400-560 (Check 5 + validateSOASerialFormat)
   - `src/lib/provisioning/auto-fix.ts` (confirm fix_soa_serial_format action exists)
4. Read `.auto-memory/feedback_hard_lessons.md` #107, #108, #109, #110, #128 for reference.
5. Read `.auto-memory/project_p14_orphan_resolution.md` — the "why savini Linodes were deleted" note. This is the new context for this run.
6. Confirm Supabase + Linode env vars are loaded (pg + linode CLI should work).
7. Sanity check: query Supabase and confirm (a) `server_pairs.id=fbc03039-00db-4b93-b996-da16c1345814` (pair_number=14, ns_domain=savini.info) still exists with status=active — this is the orphan, leave it alone; (b) there are TWO `ssh_credentials` rows for IPs `173.230.132.245` and `45.33.63.216` — these are pre-savini-attempt-1 orphans per the 2026-04-20 memory note, also leave alone.

=== PHASE 1: FIND THE P15-v2 JOB ===

The earlier P15 attempt already used pair_number=15 (failed). Do NOT filter by pair_number. Filter by time and take the newest row created since 2026-04-22T00:00:00Z:

```sql
SELECT id, pair_number, ns_domain, s1_ip, s2_ip, provisioning_job_id,
       status, created_at, updated_at
FROM server_pairs
WHERE created_at > '2026-04-22T00:00:00Z'
ORDER BY created_at DESC
LIMIT 1;
```

If no row exists yet, poll every 30s for up to 10 min. Also query `provisioning_jobs` the same way — the job row appears before the `server_pairs` row:

```sql
SELECT id, ns_domain, sending_domains, status, created_at, started_at
FROM provisioning_jobs
WHERE created_at > '2026-04-22T00:00:00Z'
  AND ns_domain != 'camire.info'    -- exclude the earlier failed P15 attempt
ORDER BY created_at DESC
LIMIT 1;
```

The expected NS domain is `lavine.info`. If you see a different ns_domain on a newer job, trust what's in the DB but record the deviation in Phase 4's report under "Residual risks".

If still nothing after 10 min, BLOCKED — Dean didn't click Provision. Write a short `reports/2026-04-22-p15-v2-launch-validation.md` saying "BLOCKED: no new provisioning_jobs row after 2026-04-22T00:00:00Z with ns_domain != camire.info" and STOP.

Once you have the job row, record: `P15V2_JOB_ID`, `P15V2_NS_DOMAIN`, `P15V2_S1_IP`, `P15V2_S2_IP`, `P15V2_PAIR_ID` (may still be null until Step 1 finishes).

Then find the saga step log. The step-log table is NOT `saga_event_log` (that doesn't exist). The real table is `provisioning_steps` — confirm with:

```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema='public'
  AND (table_name ILIKE '%provision%step%'
    OR table_name ILIKE '%saga%'
    OR table_name ILIKE '%step%log%');
```

Use whichever table has rows tied to `P15V2_JOB_ID`.

=== PHASE 2: MONITOR SAGA STEPS (poll every 30s) ===

Loop until the saga reports terminal state (SUCCESS or FAILED) or 90 min elapse. On each poll:

1. Print current step + status for `P15V2_JOB_ID`.
2. Check for these gate markers in step logs / stdout:
   - **Gate A (TZ=UTC):** On both S1 and S2, before Step 2 runs, confirm `timedatectl` returned `Etc/UTC` (or `UTC`). Search step logs for "timedatectl" / "Etc/UTC".
   - **Gate B (domain.sh patch):** After Step 2, SSH into S1 and S2 and grep:
     `grep -c 'PATCHED_BY_PROVISIONING_SAGA' /usr/local/hestia/data/templates/dns/domain.sh`
     Must return 1 on each server. The marker must be ABOVE `update_domain_zone()` (outside the echo block) — if it's inside the echo, the template is corrupt.
   - **Gate C (v-change-dns-domain-soa surfaces errors):** Look for "v-change-dns-domain-soa" in step logs. If it fires during provisioning, its exit code MUST be visible (no silent `.catch(() => {})` swallow). If exit != 0, the saga should fail-loud, not proceed.
   - **Gate D (Check 5 runs):** Verification phase must show "validateSOASerialFormat" or "Check 5" in logs. It should evaluate each zone.
   - **Gate E (fix_soa_serial_format conditional):** If any zone serial > today_UTC yyyymmdd, auto-fix must fire. If it fires, record which zones were fixed. If zero zones needed fixing, record that too (that's the ideal case — saga produced correct serials on first pass).

3. If the saga hangs > 20 min on a single step, dump that step's full stdout/stderr and flag as SUSPICIOUS but keep polling — Linode provisions can be slow.

4. If saga returns FAILED, grab the failing step's error, read the relevant handler in `src/worker/handlers/`, and write the `2026-04-22-p15-v2-launch-validation.md` report with verdict **BLOCKED — saga failed at Step N** plus root-cause analysis. THEN PROCEED TO PHASE 5 (memory updates still run on BLOCKED, just with a different verdict). Do not STOP before Phase 5.

=== PHASE 3: E2E GREEN VALIDATION (only runs if saga reached SUCCESS) ===

Run ALL of these. Any single FAIL = NO-GO for P16.

**3a. SSH parity checks on both S1 and S2:**
- `timedatectl | grep 'Time zone'` → must be `Etc/UTC` on both
- `grep -c 'PATCHED_BY_PROVISIONING_SAGA' /usr/local/hestia/data/templates/dns/domain.sh` → 1 on both
- `hostnamectl` → hostname resolves, matches server_pairs row (expected: `mail1.lavine.info` and `mail2.lavine.info`)

**3b. Zone list discovery:** For P15-v2, enumerate all zones from HestiaCP on S1:
`v-list-dns-domains admin | awk 'NR>2 {print $1}'`

Expect 11 zones (lavine.info NS + 10 sending: carosi/cirone/lodema/luvena/marife/morice/norita/renita/valone/verina, all .info). Save list as `P15V2_ZONES[]`.

**3c. AXFR parity (dig @S1 == dig @S2) for every zone:**
```
for z in "${P15V2_ZONES[@]}"; do
  dig @$P15V2_S1_IP $z AXFR +nocookie +time=10 > /tmp/s1-$z.axfr
  dig @$P15V2_S2_IP $z AXFR +nocookie +time=10 > /tmp/s2-$z.axfr
  diff /tmp/s1-$z.axfr /tmp/s2-$z.axfr && echo "AXFR OK: $z" || echo "AXFR FAIL: $z"
done
```
Any mismatch → BLOCKED.

**3d. verify-zone.sh canonical check** (tools/verify-zone.sh is intoDNS-style):
```
for z in "${P15V2_ZONES[@]}"; do
  bash tools/verify-zone.sh "$z" || echo "VERIFY FAIL: $z"
done
```
Count FAILs. FAIL=0 required.

**3e. SOA serial format validation — re-run the Check 5 logic out-of-band:**
For each zone, resolve its SOA serial and assert `yyyymmdd_part <= today_UTC_yyyymmdd`. Any zone with serial > today = auto-fix didn't fire or didn't take. BLOCKED.

**3f. MXToolbox Domain Health on all 11 P15-v2 zones.** Use Chrome MCP (tabId from the current browser session — if no active tab, open a new one to mxtoolbox.com/domain/). Scan each zone's domain health page. Record for each: Errors, Warnings, Passed. Required: **0 Errors AND 0 Warnings on all 11 zones.** Any SOA Serial warning = auto-fix chain failed on the saga run. BLOCKED. (Per HL #110 MXToolbox UI is advisory for most checks, but the SOA Serial Format warning is a HARD FAIL here because it's the entire reason PR #18 exists.)

**3g. Mail-tester spot check** — pick 3 of the 10 sending domains at random. For each, send a test email through the HestiaCP account to the fresh mail-tester address and capture score. Required: 10/10 on each (or document any point deduction). This is advisory for the verdict (not a hard blocker) unless score < 9/10.

=== PHASE 4: WRITE THE REPORT ===

File: `dashboard-app/reports/2026-04-22-p15-v2-launch-validation.md`

Structure:
1. **Verdict line** at top: `## VERDICT: READY FOR P16` or `## VERDICT: BLOCKED — <reason>`
2. **Saga timing table** — each step with start/end/duration
3. **Gate A-E pass/fail table**
4. **E2E GREEN results table** — one row per check (3a-3g), Pass/Fail/Notes
5. **Per-zone table** — zone name, AXFR parity, verify-zone.sh result, SOA serial, MXToolbox (E/W/P), mail-tester (if sampled)
6. **Auto-fix firings** — list every zone that needed fix_soa_serial_format, with before/after serial
7. **Residual risks** — anything weird you saw that didn't cross the FAIL line but is worth noting for P16

=== PHASE 5: MEMORY UPDATES (ALWAYS RUN — verdict-conditional content) ===

Memory ownership per CLAUDE.md:
- **Project 8 (Master) owns:** MEMORY.md, feedback_*.md, reference_*.md — you MAY edit these directly.
- **Project 1 (HestiaCP) owns:** project_server_deployment.md — you MAY edit because this is a legitimate cross-project update recording a saga outcome (HL #102 "canonical pair identity comes from server_pairs live, not archive filenames").
- **Project 9 (Dashboard) owns:** project_saas_dashboard.md — you MAY edit for Gate 0 status change.
- Do NOT touch project files owned by other projects (Lead Gen, Email Copy, GHL, etc.).

**5a. MEMORY.md** — append a new top-line dated entry (under the existing 2026-04-22 entries) with the P15-v2 verdict:
```
*2026-04-22 — **P15-v2 <VERDICT>.** Saga job_id=<P15V2_JOB_ID>, pair_id=<P15V2_PAIR_ID or null>, NS=lavine.info, S1=<S1_IP> S2=<S2_IP>. PR #18 (7b64ad3) SOA-fix chain <validated end-to-end | not reached — blocked at Step N>. Fix_soa_serial_format fired on <zones list | 0 zones (clean first pass)>. MXToolbox UI Errors/Warnings across 11 zones: <totals>. mail-tester sample: <scores>. Full report: dashboard-app/reports/2026-04-22-p15-v2-launch-validation.md. <READY FOR P16 | NO-GO for P16: root cause summary>.*
```
Keep MEMORY.md under 200 lines — if needed, trim older dated entries (archive the text in a file under `.auto-memory/archive/` first).

**5b. project_server_deployment.md** — if VERDICT=READY, add a new `Pair 15 — lavine.info` row matching the existing P13/P14 pattern (ns_domain, S1/S2 IPs, hostnames, 11 zones, status=LIVE, date verified). If VERDICT=BLOCKED, add a terse "Pair 15 attempt 2 halted at Step N on 2026-04-22 — see report" line under a "Halted attempts" sub-heading (create the sub-heading if it doesn't exist).

**5c. project_saas_dashboard.md** — update the Gate 0 line. If GREEN: "Gate 0 SAGA-VALIDATED via P15-v2 on 2026-04-22 (job <P15V2_JOB_ID>, PR #18 @ 7b64ad3 exercised end-to-end, all 11 zones 0E/0W). Next: Gate 1 B16 merge." If BLOCKED: "Gate 0 remains BLOCKED — P15-v2 saga halted at Step N on 2026-04-22, root cause: <summary>. Retry required before Gate 1."

**5d. feedback_rebuild_assessment.md** — search for any "canary DNSBL fix" / "Gate 0 BLOCKING" / "Gate 0 BLOCKED" references and update them with the new verdict. If GREEN, flip them to "SAGA VALIDATED on 2026-04-22 via P15-v2 end-to-end run (PR #18 @ 7b64ad3)". If BLOCKED, append the new blocker on top with the date.

**5e. feedback_hard_lessons.md — APPEND ONLY IF the saga surfaced a NEW, non-obvious lesson.** Examples that warrant a new HL:
- Gate D unexpectedly skipped for a specific zone condition
- fix_soa_serial_format produced the correct serial but MXToolbox still flagged it because of a second-order cache issue
- A Linode provisioning race condition that repeated twice and was not covered by MAX_IP_REROLL_ATTEMPTS
Examples that DO NOT warrant a new HL: a single Linode timeout that retried successfully, MXToolbox UI warnings that HL #110 already covers, a saga step running longer than expected but completing.
If appending, number it as the next available (current top is #135 per MEMORY.md). Write the HL in the existing format: heading `## NNN. Title (YYYY-MM-DD)` + body with rule + evidence + remediation.

=== PHASE 6: COMMIT ===

```
cd dashboard-app
git add reports/2026-04-22-p15-v2-launch-validation.md
git add ../.auto-memory/MEMORY.md ../.auto-memory/project_server_deployment.md ../.auto-memory/project_saas_dashboard.md ../.auto-memory/feedback_rebuild_assessment.md
# include feedback_hard_lessons.md ONLY if Phase 5e added a new HL
git commit -m "P15-v2 saga validation <GREEN|BLOCKED> 2026-04-22 + memory sync"
```
(Note: .auto-memory lives outside the dashboard-app git repo; if `git add ../.auto-memory/...` errors with "outside the repository", commit only the report inside dashboard-app and leave the .auto-memory/*.md edits uncommitted — they're saved on disk and will be picked up by the next Master session. Record that in the final chat message.)

Do NOT push unless Dean already configured auto-push on the worker.

=== FINAL MESSAGE ===

End your run with a single-paragraph summary for Dean: verdict, what passed, what blocked (if anything), memory files updated, commit hash (if created), and the one-line "Dean, click Provision on P16" or "Dean, do NOT click P16 — see report". Include the P15V2_JOB_ID and P15V2_PAIR_ID for quick lookup.

Rules while running:
- Use parallel bash only for independent reads (AXFR from S1 and S2 for the SAME zone, different zones' MXToolbox scans). Never parallelize DB writes or SSH config reads on the same host.
- If anything surprises you, STOP, read the relevant handler source, and reason from the code — do not guess.
- Convert any relative dates in your report to absolute UTC dates.
- DO NOT touch the P14 savini orphan rows (server_pairs id=fbc03039-00db-4b93-b996-da16c1345814, ssh_credentials IPs 173.230.132.245 + 45.33.63.216). They are deliberately left alone.
- DO NOT "restore" savini.info as a reusable ns_domain — it is burnt from a server_pairs uniqueness and orphan posture standpoint.
- DO NOT provision P16 under any circumstance. That's Dean's click.
```

---

## Notes for Dean (don't paste this part)

**Session settings:** New CC session. On `main` (not a worktree — P15-v2 is read-only monitoring + report write + memory update, no risk of breaking a parallel stream). Effort = ultrathink. Enable all permissions (bash, edit, SSH, DB).

**Why this is safe on main:** The monitor only reads the saga, reads zones via dig/SSH, runs MXToolbox via Chrome MCP, writes one report file, and appends to memory files. It does not mutate the saga, the worker, or any provisioning code. If a saga bug shows up that needs a code patch, CC should stop at the report-write + memory-update phases with a BLOCKED verdict — do NOT let it patch code in the same session.

**What to do before clicking Provision on P15-v2:**
1. Confirm Vercel and worker both on `7b64ad3` or later (PR #18 merged — already the case per `2026-04-22-p14-parity-complete.md`).
2. Confirm Dean has 2 fresh Linodes ready (freed from deleting P14's S1 + S2).
3. Confirm the 11 selected domains exist in the Namecheap account (they do — all registered 2026-04-22 per `2026-04-22-namecheap-inventory-p15-v2.md`).
4. In the dashboard Provision form, set NS=`lavine.info`, sending=[carosi, cirone, lodema, luvena, marife, morice, norita, renita, valone, verina].info, provider=linode, region=us-central/us-southeast, admin=`dean.hofer@thestealthmail.com`.

**Namecheap NS/glue configuration:** fully handled by saga Step 3 (`configure_registrar`) in `src/lib/provisioning/registrars/namecheap.ts`. No manual registrar touch required.

**If the report comes back READY FOR P16:** you have a validated hands-free chain. P16 should need zero human intervention beyond clicking Provision (on a new NS domain from the 11 RESERVED Wayback-present domains after a content-review pass).

**If the report comes back BLOCKED:** read the root-cause section. Do NOT click P16. Open a new CC session to fix the specific failure and re-run this monitor on a re-provisioned P15 (delete the P15-v2 Linodes, re-click Provision on a new ns_domain from the reserved set, re-run this monitor prompt).
