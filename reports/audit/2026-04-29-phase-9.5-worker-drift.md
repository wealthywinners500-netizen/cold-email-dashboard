# Phase 9.5 — Worker VPS cross-infra drift report

**Generated:** 2026-04-28 (V4 streamlined finish, Phase 9.5 lane 3 of 4)
**Method:** SSH `root@200.234.226.226` (pubkey-authed since 2026-04-23)
**Scope:** `/opt/dashboard-worker` daemon + systemd state + env keys + pg-boss queue topology

## Summary

Worker is healthy and on main HEAD. **F-16 deeper finding CONFIRMED**: `poll-provisioning-jobs` queue has **NO worker handler registered** — every other queue is registered, only this one is missing. The cron emits ~1 job/min into a void; pg-boss retention sweeps at 14d.

## Findings

### F-NEW-9.5-WORKER-1 — `poll-provisioning-jobs` queue handler is missing (P2 → bumped from F-16)

**Trigger:** F-16 deeper observation from Phase 7-collapsed; Phase 9.5 worker probe confirms.

**Evidence:** `grep -nE 'boss\.work\(' /opt/dashboard-worker/src/worker/index.ts` returns 10 handler registrations:
```
160:  await boss.work("sync-all-accounts", async () => {
172:  await boss.work("classify-batch", async () => {
184:  await boss.work("reset-daily-counts", async () => {
210:  await boss.work("check-no-reply", async () => {
245:  await boss.work("queue-sequence-steps", async () => {
257:  await boss.work("warm-up-increment-cron", async () => {
269:  await boss.work("smtp-connection-monitor", async () => {
281:  await boss.work("account-deliverability-monitor", async () => {
293:  await boss.work("campaign-performance-monitor", async () => {
305:  await boss.work("distribute-campaign-sends", async () => {
```

**`poll-provisioning-jobs` is NOT in this list.** Yet the queue exists in pg-boss (`pgboss.queue` row with `queued_count=21522`, oldest job 14 days old, newest 1 minute old) — meaning something IS emitting jobs into it but nothing IS consuming them.

**Source of emit (deferred investigation):** likely a Vercel cron OR a `boss.send` call from somewhere else (not a worker-side `boss.work`). Need to grep `boss.send.*poll-provisioning-jobs` across both `dashboard-app/src/` and `/opt/dashboard-worker/` to find the producer.

**Recommendation:** queued for post-audit Workstream 4 (P2 recommission) per `2026-04-29-FIX-BACKLOG.md` Side-1. Not blocking Phase 8 / Phase 10 closure.

### Worker state (matches expected)

| Field | Actual | Expected | OK |
|---|---|---|---|
| `cd /opt/dashboard-worker && git rev-parse HEAD` | `00b3260de60e3df27e8140e424b7664f4700a5d0` | main HEAD `00b3260` | ✓ |
| systemctl status | `active` | active | ✓ |
| `ActiveEnterTimestamp` | Mon 2026-04-27 20:33:06 CEST (~30 hr uptime) | continuous since last deploy | ✓ |
| MainPID | 301074 | (assigned by systemd) | — |
| RSS | 86 MB | (typical for tsx + pg-boss + worker handlers) | ✓ |
| ExecStart | `/usr/bin/npx tsx --tsconfig tsconfig.json src/worker/index.ts` | same | ✓ |
| Disk usage | 31% (7.3G / 24.5G) | <80% headroom | ✓ |

### Env-keys present (15 keys, alphabetical)

Names only (no values per Hard Rule #9):
```
DATABASE_URL, ENABLE_LEGACY_MONOLITHIC_PROVISIONING, ENCRYPTION_KEY,
IONOS_API_PREFIX, IONOS_API_SECRET, LINODE_API_KEY,
MXTOOLBOX_API_KEY, NEXT_PUBLIC_APP_URL,
NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_SUPABASE_URL,
NODE_ENV, SPAMHAUS_DQS_KEY, SUPABASE_SERVICE_ROLE_KEY,
WORKER_CALLBACK_SECRET, WORKER_ROLE
```

15 keys — matches V4 plan expected count. **No `NAMECHEAP_*` keys on the worker** — F-21 P2 finding (Vercel-only) is correct: worker doesn't read Namecheap directly; provisioning goes through DB-driven creds.

DATABASE_URL is the canonical session-mode pooler value (port 5432) per HL #153 — correct for the long-lived worker process. Vercel uses transaction-mode (port 6543) for serverless invocations — added Phase 8.1.

### F-18 confirmed: untracked debug files in `/opt/dashboard-worker/`

Sample (first 20 of 67):
```
.env, .p14-observe.js, .p14-part0.js, _pb.mjs, _pb2.mjs, _pb3.mjs,
_st.mjs, _st2.mjs, _st3.mjs, _st4.mjs, _st5.mjs, _st7.mjs, _st8.mjs,
_trigger.ts, break_deadlock.mjs, cancel_jobs.js, check-status-constraint.mjs,
check-status-vals.mjs, check.js, check.mjs
```

Cleanup item per FIX-BACKLOG. Not blocking.

### Recent journalctl errors (last 30 min)

10 IMAP TLS Hostname/IP cert mismatch errors — same shape as F-17 (customer-side IMAP TLS issues, 57k+ over 7d). Examples:
```
[IMAP] Error syncing timothy.graves@verina.info: Hostname/IP does not match certificate's altnames: IP: 45.79.111.103 is not in the cert's list
[IMAP] Error syncing peter.brown@searely.info: Hostname/IP does not match certificate's altnames: IP: 50.116.14.26 is not in the cert's list
```

Pattern: customer IMAP clients connecting by IP rather than hostname; cert is for `mail.<sd>` not the IP. Customer-side configuration issue, not worker-side. F-17 already cataloged as P2 (non-blocking).

## Drift summary

- **NEW finding (F-NEW-9.5-WORKER-1):** `poll-provisioning-jobs` queue handler missing — investigation queued for Workstream 4 per FIX-BACKLOG.
- **OK:** git HEAD matches main, systemctl active 30h, env keys complete, F-22 `commandForIgnoringBuildStep` not relevant on worker.
- **Carry-forward:** F-17 (IMAP TLS), F-18 (67 untracked debug files) — both already cataloged.

**No HALT recommended from this lane** (the new finding is a deeper observation of an already-cataloged F-16; backlog handles it).
