# Phase 0.5 — Pair Verify status (R4)

**Generated:** 2026-04-29 (CC audit, branch `claude/vibrant-golick-ce8da1`)
**Source:** live `pair_verifications` query (table currently has 18 rows total; latest-per-pair extracted)

## Schema drift surfaced first

The audit prompt referenced `pair_verifications.server_pair_id` — that column does **not** exist. Actual column is `pair_id`. P0 finding for the prompt template + any code site that may use the same wrong name (Phase 4.2.a will grep).

## Latest verification status per pair

| Pair | ns_domain | Latest PV status | run_at |
|---|---|---|---|
| P1 | grocerysynergy.info | **red** | 2026-04-27T18:40:53Z |
| P2 | krogernetworks.info | **red** | 2026-04-21T11:50:28Z |
| P3 | krogertogether.info | **red** | 2026-04-21T11:55:03Z |
| P11 | caleong.info | green | 2026-04-20T18:44:26Z |
| P12 | launter.info | green | 2026-04-20T16:47:26Z |
| P13 | launta.info | green | 2026-04-20T16:47:33Z |
| P14 | camire.info | **NEVER VERIFIED** | n/a |
| P15 | lavine.info | **NEVER VERIFIED** | n/a |
| P16 | mareno.info | **NEVER VERIFIED** | n/a |
| P17 | cemavo.info | **NEVER VERIFIED** | n/a |
| P18 | partnerwithkroger.store | yellow | 2026-04-24T22:24:12Z |
| P19 | marketpartners.info | green | 2026-04-25T15:23:33Z |
| P20 | krogermedianetwork.info | **red** | 2026-04-27T20:11:39Z |
| P21 | krogermarketinggroup.info | **red** | 2026-04-27T20:11:29Z |

## Findings vs HANDOFF_2026-04-28.md

1. **P20 + P21 — HANDOFF says "saga GREEN, Pair Verify NOT YET CLICKED, never warmed"; reality says PV was clicked on 2026-04-27 and came back RED on both pairs.** PV oracle disagreement with HANDOFF is a P0 finding for the audit. Phase 5 should not assume P20/P21 are clean infra — the most recent oracle reading says otherwise. Dean's mental model needs reconciliation.

2. **P14 / P15 / P16 / P17 — zero pair_verifications rows.** Saga completed (`provisioning_jobs` rows show status=completed for each of these), but the Pair Verify oracle was never run. HANDOFF treats them as "Linode, ready, never warmed" — true, but the green-light from PV that golden-saga implies was never recorded. CSV-scope candidates if Dean accepts shipping warm-up for never-verified pairs, or hold-back candidates if he wants PV-green prerequisite.

3. **P19 PV status is GREEN despite 0 accounts + 12 sending_domains.** PV oracle only inspects DNS/cert/PTR plumbing — it has no notion of "but this pair has zero email accounts to send from". Phase 5a will dig into the account-presence mystery; the green PV is consistent with infra existing but accounts being absent.

4. **P18 yellow — matches HANDOFF (DBL pending on 8 burnt domains) and the all-30-disabled state.** Phase 5b will dig further.

5. **P1 / P2 / P3 red — Clouding-era pairs.** Expected; Clouding cluster has known DBL listings + propagation issues that PV catches. Not a new finding.

## CSV-phase implication

Per Dean's CSV scope (active except P18+P19, P1/P3 empty so excluded):
- **P2** — PV red, but it's the only Clouding pair with accounts (30); HANDOFF flags port-25-history; CSV-gen safe but pre-flight (Phase 3.1) will be the real gate.
- **P11 / P12 / P13** — PV green, oldest verifications (2026-04-20). Re-running PV before warm-up would tighten the proof, but state hasn't been touched since then so green is still defensible.
- **P14 / P15 / P16 / P17** — never-verified. Phase 3.1 SMTP/IMAP preflight per pair (real send + STARTTLS + AUTH PLAIN to a test mailbox + IMAP SSL probe) is the per-pair gate that will catch any cert/auth/TLS issue. Acceptable to proceed; surface clearly to Dean.
- **P20 / P21** — most recent PV is RED. Need to identify the RED reason (which check failed) before Phase 3 fires. Almost certainly the same cause for both since they were verified seconds apart. Quick read of the latest PV row's `checks` JSON (Phase 5b can absorb this) will show.

## Advisory (not a HALT)

This phase is read-only by design. Dean can queue a fresh Pair Verify click for any of P14/P15/P16/P17/P20/P21 in the dashboard before Phase 3 fires if he wants the oracle green-light. CC does not click the wizard (Clerk-gated).

## Action for Phase 5b — DONE in-line (resolved during Phase 0.5)

Pulled the full `checks` JSON for the latest P20 + P21 rows. **Only one check failed on each, identical on both:**

```
name=enqueue_failed  result=fail
detail: {"error": "DATABASE_URL environment variable is required for pg-boss"}
```

All real infrastructure checks pass on both pairs:
- `mxtoolbox_domain_health` — pass
- `multi_resolver_ptr` — pass
- `dns_propagation` — pass
- `operational_blacklist_sweep` — pass

**Verdict:** P20 + P21 RED status is a **dashboard-side environment-variable bug**, NOT a real pair-health defect. Real infra is green on both. The Pair Verify oracle attempts to enqueue a follow-up pg-boss job, that enqueue fails because `DATABASE_URL` is missing in the runtime environment, the failure gets recorded as a check, the overall scoring marks the run RED.

**P0 fix candidate for Phase 8:**
1. The Pair Verify route's pg-boss enqueue path requires `DATABASE_URL` but Vercel production env doesn't have it (or has a wrong name; worker uses a different env name like `WORKER_DATABASE_URL`). Phase 4.1.c will diff env-var keys between Vercel prod and worker `.env`.
2. Scoring bug: a plumbing-failure check (`enqueue_failed`) shouldn't outweigh four passing infra checks and flip the whole pair to RED. Either (a) re-classify enqueue failures as warnings, (b) skip enqueue on the verify path, or (c) fix the env var so it stops failing.

**Action for Dean:** No PV re-click needed yet — running again without an env fix will produce the same false-RED. Audit Phase 8 will surface the env-key proposal for Dean's ack before any change lands.
