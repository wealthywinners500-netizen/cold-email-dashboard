# Phase 9.5 — Clerk cross-infra drift report

**Generated:** 2026-04-28 (V4 streamlined finish, Phase 9.5 lane 2 of 4)
**Method:** Clerk REST API (`api.clerk.com`) via `CLERK_SECRET_KEY`

## Summary

**ONE CRITICAL FINDING:** the `org_id` in `.auto-memory/reference_credentials.md` line 67 is **wrong**. The actual Clerk-side org ID differs by 3 characters from the documented ID. The 2026-04-21 audit's stated "correction" went the wrong direction.

## Findings

### F-NEW-9.5-CLERK-1 — `reference_credentials.md` Clerk org_id is incorrect (P1)

**Documented value (`.auto-memory/reference_credentials.md:67`):**
```
org_3C2CaqUMSFDyZ9wTXmvFRGPoO0q
       ^^^                  ^^^
       (positions 9-10)    (last 3 chars)
```

The 2026-04-21 audit comment claims: "ends `O0q` = capital-O + digit-zero + lowercase-q".

**Actual value (Clerk API LIST `/v1/organizations` response, name=StealthMail):**
```
org_3C2CaQuMSFDyZ9wTXmvFRGPoOOq
       ^^^                  ^^^
       (positions 9-10)    (last 3 chars)
```

Actually ends `OOq` = **capital-O + capital-O + lowercase-q** (NOT `O0q` digit-zero).

**Character-by-character delta:**

| Position | Documented | Actual | Difference |
|---|---|---|---|
| 9 | `q` (lowercase) | `Q` (uppercase) | case |
| 10 | `U` (uppercase) | `u` (lowercase) | case |
| 28 (3rd from end) | `0` (digit zero) | `O` (capital-O) | character |

**Verification:**
- Direct `GET /v1/organizations/org_3C2CaqUMSFDyZ9wTXmvFRGPoO0q` (the documented value) returns:
  ```json
  {"errors":[{"message":"not found","long_message":"Resource not found","code":"resource_not_found"}]}
  ```
- `GET /v1/organizations?limit=10` (LIST endpoint) returns 4 orgs; the StealthMail one is `org_3C2CaQuMSFDyZ9wTXmvFRGPoOOq`.

**Impact:** any code that hardcodes the documented org_id will 404. Any join/filter using the documented value will return zero rows. The dashboard's Clerk integration uses `auth().orgId` (which is the live Clerk session's org claim, not a hardcoded constant), so the dashboard runtime is OK — but anywhere a CC operator copy-pastes the org_id from credentials into a script will fail.

**Phase 9.4 patch proposal** (folded into `2026-04-29-MEMORY-PROPOSAL.md` File 3 — `reference_credentials.md`):

```diff
-- **Dean's Org:** StealthMail — org_3C2CaqUMSFDyZ9wTXmvFRGPoO0q (created 2026-04-07; corrected 2026-04-21 audit — ends `O0q` = capital-O + digit-zero + lowercase-q)
+- **Dean's Org:** StealthMail — `org_3C2CaQuMSFDyZ9wTXmvFRGPoOOq` (created 2026-04-07; **2026-04-29 audit re-correction:** ends `OOq` = capital-O + capital-O + lowercase-q. The 2026-04-21 "correction" was itself wrong — both positions 9-10 case-swapped (was `qU` should be `Qu`) AND last-3 had `0` (digit zero) where actual is `O` (capital-O). Verified against `GET /v1/organizations` LIST endpoint 2026-04-29 — direct GET on the old documented value returns `resource_not_found`.)
```

**Effort:** propose only in this audit (CC applies to `reference_credentials.md` at the 9.7 HALT per per-file ack). Dean confirms the API LIST result is canonical.

### Instance metadata (matches dev environment)

| Field | Actual | Expected | OK |
|---|---|---|---|
| `id` | `ins_3BxKYzGwRDfKH6itOqTSJuUrIuc` | (instance ID, distinct from app ID) | ✓ |
| `environment_type` | `development` | `development` | ✓ |
| Slug (from publishable key) | `charming-dane-14` (per credentials reference) | same | ✓ |
| App ID (per credentials) | `app_3BxKYyPkfj5yN6eSW6LUX9ygem0` | same | ✓ |

Note: instance ID (`ins_...`) differs from app ID (`app_...`) — they're separate Clerk concepts. Both are documented correctly in `reference_credentials.md`.

### JWT template (matches expected)

| Field | Actual | Expected | OK |
|---|---|---|---|
| count | 1 | 1 | ✓ |
| id | `jtmp_3BxNXaJyQC3CYhb1pbxXAvE6JyF` | same | ✓ |
| name | `supabase` | same (Supabase RLS integration) | ✓ |

### Allowlist domains

`/v1/allowlist_identifiers` returns 0 entries. For a development instance, this is normal — sign-up is open without an allowlist gate. Production instance would typically have one.

### Other organizations on this Clerk instance

The LIST returned 4 orgs:
- `org_3CBQc786Dng2S4Mb4emugpbemLl` — Zachary's Organization (test)
- `org_3C9t3XRX94HHvnkOLXr91H3snpK` — Cool's Organization (test)
- `org_3C9nmUSC4fcivp9wmmazL4Y5tmp` — Mayank's Organization (test)
- `org_3C2CaQuMSFDyZ9wTXmvFRGPoOOq` — StealthMail (Dean's, the production-equivalent org)

3 test orgs from various users — likely from the dev instance's sign-up testing. Not a concern.

## Drift summary

- **CRITICAL:** F-NEW-9.5-CLERK-1 — credentials reference has wrong org_id (3-char delta). Apply patch via `2026-04-29-MEMORY-PROPOSAL.md` File 3 at the 9.7 HALT.
- **OK:** instance ID, environment type, JWT template, app ID — all match documented state.
- **Observation:** 3 test orgs exist on the dev instance — not a concern; clean up if desired but not required.

**HALT recommended after Phase 9.5 close** to surface F-NEW-9.5-CLERK-1 explicitly to Dean — this is a production-critical credential reference correction and needs his eyes before CC applies the patch.
