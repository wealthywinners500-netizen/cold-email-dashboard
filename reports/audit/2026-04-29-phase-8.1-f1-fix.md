# Phase 8.1 — F-1 fix: DATABASE_URL added to Vercel prod env

**Generated:** 2026-04-28 (V4 streamlined finish)
**Status:** APPLIED + behaviorally verified
**Commit:** atomic, this report bundled

## Action

1. POST `/v9/projects/prj_anAbzLwY19mCmQcblypDQQYDQjGg/env`
   - key=`DATABASE_URL`, target=`production`, type=`encrypted`
   - env id assigned: `X2Xjg3qtO5sjIvqb`
   - total prod envs: 22 → 23
2. **Mid-step refinement:** initial value (worker session-mode pooler at port 5432) caused PV API to return 500 with `MaxClientsInSessionMode: max clients reached - in Session mode max clients are limited to pool_size`. PATCHed env id `X2Xjg3qtO5sjIvqb` to flip port 5432 → 6543 (transaction-mode pooler — correct for Vercel serverless).
3. Two redeploys via `POST /v13/deployments?forceNew=1` with `deploymentId` source = latest READY (`dpl_k6dWZ9MgHwfvcVssCpR3Na4DjDF2`):
   - First (post-add): `dpl_HmcL6NfgGEMUTeEfra6uF7yVoaHx` READY at +234s
   - Second (post-PATCH): `dpl_2CaZAy9kYZkgHJutt2fmgcwbVrW7` READY at +218s

Plaintext value handling: SSH worker → Python subprocess capture → in-memory string (port-swap via `urllib.parse.urlparse`) → urllib `data=` body. Value never logged to chat, commit message, report markdown, temp file, or shell argv.

## Behavioral verification

POST `/api/pairs/dd387138-4f41-42a1-8217-bb3e3702b9aa/verify` (P11 caleong.info, admin Clerk session):

| Run | Status | Body | Notes |
|---|---|---|---|
| Pre-fix | 500 | `{"error":"Failed to enqueue verification job"}` | F-1 active |
| Post-add (port 5432) | 500 | same — backing PV row had `checks[0].name=enqueue_failed`, error=`MaxClientsInSessionMode...` | wrong pooler mode |
| Post-PATCH (port 6543) | **202** | `{"verificationId":"b1799c53-6154-4961-8c72-53278e62ddd1"}` | F-1 cleared |

Worker processed the b1799c53 job end-to-end: `duration_ms=8259`, `completed_at` populated, 5 real check rows (intodns_domain_health, mxtoolbox_domain_health, multi_resolver_ptr, dns_propagation, operational_blacklist_sweep). `status='red'` is from a real `intodns_domain_health: fail` on caleong.info — actual P11 health issue, NOT queue infra.

`pair_verifications` total: 18 → 20 (post-add failed run + post-PATCH success run).

## Architecture note

Same Supabase Supavisor cluster, two pooler modes:

| Surface | Port | Mode | Why |
|---|---|---|---|
| Worker daemon (`/opt/dashboard-worker`) | 5432 | session | long-lived process; needs LISTEN/NOTIFY for pg-boss consume; supports prepared statements + session state |
| Vercel API routes / serverless | 6543 | transaction | short-lived invocations; pool releases connection at transaction boundary; supports thousands of concurrent function instances |

**Same DATABASE_URL except port. Don't propagate one consumer's value to another consumer's environment.**

The audit prompt's "use the worker's value as canonical" instruction is wrong for Vercel — it inherited V4's paste-back framing which assumed pooler mode is per-project not per-consumer-surface. **Phase 9.1 audit-prompt patch queued** (item +1 to the patch list, now 9 items). **HL #153 candidate queued for Phase 9.2** (pooler mode is per-consumer-surface, not per-project).

## Side observations (post-audit backlog, not Phase 8 scope)

- `src/app/api/pairs/[id]/verify/route.ts` swallows distinct pg-boss error classes under generic `enqueue_failed`. The new PV row's `checks[0].details.error` did surface the real message (`MaxClientsInSessionMode...`), so the path works but the error name is misleading. Suggest a separate check name (`pg_boss_send_error`) or richer error classification.
- `src/lib/email/campaign-queue.ts:22-35` may already document the pooler-split pattern as an internal "HL #R3" comment from 2026-04-18. Phase 9.2 grep step will confirm whether this is already promoted to `.auto-memory/feedback_hard_lessons.md` or needs to land as HL #154.

## Saga / branch hygiene

- Saga golden tag `p16-golden-saga-2026-04-23` sha unchanged at `c1cc0bf96f7aed54a5e74c0f5cf20cb693263de1`.
- F-24 baseline holds (tag..HEAD diff = 1 file, `auto-fix.ts` only).
- Audit isolation invariant holds (main..HEAD diff = 0 saga files).
- This commit only adds reports under `reports/audit/`. Zero src/ touches.
