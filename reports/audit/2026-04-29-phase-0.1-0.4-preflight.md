# Phase 0.1–0.4 — Pre-flight (saga + tests + HEAD + Supabase)

**Generated:** 2026-04-29 (CC audit, branch `claude/vibrant-golick-ce8da1`)
**Status:** complete; all hard checks pass.

## 0.1 — Saga golden-tag verification ✓

```
$ git show-ref -s p16-golden-saga-2026-04-23
c1cc0bf96f7aed54a5e74c0f5cf20cb693263de1
```

Matches the expected sha exactly. **Saga isolation invariant intact.**

`src/__tests__/dbl-resweep-saga-isolation.test.ts` confirms the same:
```
[saga-isolation] base=origin/main files-changed=0
PASS: (a) zero saga files modified by this branch
PASS: (b) zero provisioning/pairs UI or API paths modified
```

## 0.2 — 16-gate test snapshot ✓

`npm run test:gate0` exit code 0. **Note:** the prompt referenced "16 tests"; the actual `package.json` `test:gate0` script chains **23** tests inline (added since the prompt was authored). All 23 PASS.

Tests run (inline `&&` chain):
1. dnsbl-liveness · 2. ip-blacklist-check · 3. saga-cascade-guard · 4. pair-verify · 5. await-auth-dns
6. vg2-ssl-probe-mail-hostname · 7. mx-spec-alignment · 8. zone-sync-axfr · 9. vg2-fcrdns-resolver
10. pair-verify-invaluement-dead-zone · 11. vg2-sending-domain-web-cert · 12. setup-mail-domains-ns-apex
13. verify-dkim-cross-server-match-ns · 14. patch-10d-axfr-zone-ownership · 15. pair-detail-client
16. audit-remediation-invariants · 17. vg2-retry-backoff · 18. soa-serial-format · 19. smtp-pass-reader
20. fixsoa-mname-explicit · 21. saga-step9-chmod-cert-key · 22. dbl-resweep · 23. dbl-resweep-saga-isolation

Notable assertions in the green output (proves the on-branch state is correct):
- `smtp-pass-reader`: ssh_credentials read path + missing-row throw + null-pw throw + supabase-error throw all green (HL #133/#138 helper).
- `fixsoa-mname-explicit`: fixSOA renders `ns1.${nsDomain}` literal MNAME with the HL #107 timers, never two-empty-arg `'' ''` (HL #145-companion guard).
- `saga-step9-chmod-cert-key`: `issueSSLCert` `isHostname` branch enforces `chmod 0640` + `chown Debian-exim:mail` post-`v-add-letsencrypt-host`, with a defensive `stat -c %a` warn-not-throw (HL #145).
- `dbl-resweep`: 5 cases including org-scoped invariant — Clouding-imported pair NOT probed by default cron, only via explicit override.

**No drift in the saga or saga-adjacent test layer.**

## 0.3 — Three-way HEAD alignment ✓

| Source | sha |
|---|---|
| `git ls-remote origin main` | `00b3260de60e3df27e8140e424b7664f4700a5d0` |
| this worktree HEAD | `00b3260de60e3df27e8140e424b7664f4700a5d0` |
| Vercel current production deploy (`prj_anAbzLwY19mCmQcblypDQQYDQjGg`) | `00b3260d…` (state=READY) |
| Worker `/opt/dashboard-worker` HEAD (root@200.234.226.226) | `00b3260de60e3df27e8140e424b7664f4700a5d0` |

**All four sources agree.** No drift. Worker hostname returns `mail1.partner-with-kroger.info` — known drift documented since 2026-04-27 in HANDOFF, not a new issue.

Vercel had the previous deploy `6f9b3179` (PR #20 saga fixSOA chmod) just before; current production `00b3260` is the PR #21 weekly DBL re-sweep merge. State=READY on all 3 most-recent production deploys.

## 0.4 — Live Supabase ground-truth probe ✓

| Table | Live count | Expected (HANDOFF_2026-04-28) | Match |
|---|---|---|---|
| `server_pairs` | 14 | 14 | ✓ |
| `email_accounts` (active) | 288 | 288 | ✓ |
| `email_accounts` (disabled) | 30 (all P18) | 30 (all P18) | ✓ |
| `sending_domains` (clean) | 111 | 111 | ✓ |
| `sending_domains` (burnt) | 8 (all P18) | 8 (all P18) | ✓ |
| `pair_verifications` | 18 total rows | n/a | new probe |
| `provisioning_jobs` (last 10) | last 3 are failures (2 unattached, 1 P18 rolled_back) | n/a | new probe |

**Zero overnight drift since HANDOFF.** All counts match.

### Drift findings surfaced

**DRIFT-1 (schema):** `pair_verifications.server_pair_id` does not exist. Actual column is `pair_id`. The audit prompt referenced `server_pair_id` (would have errored every PV query). Phase 4.2.a should grep `src/` to confirm code uses the right name (likely correct in code; just prompt-template drift). P3 finding.

**DRIFT-2 (state vs HANDOFF — Pair Verify):** HANDOFF tier table claims "P20 + P21 saga GREEN, Pair Verify NOT YET CLICKED, never warmed." Live data: PV WAS clicked on 2026-04-27 and returned RED on both. Root cause is a dashboard env-var regression (`DATABASE_URL` missing in Vercel prod), not a pair-quality defect. Detail in `2026-04-29-phase-0.5-pair-verify-status.md`. P0 fix candidate for Phase 8.

**DRIFT-3 (state vs HANDOFF — never-verified pairs):** P14 / P15 / P16 / P17 have ZERO pair_verifications rows. Saga completed cleanly (`provisioning_jobs.status='completed'` for each), but the Pair Verify oracle was never run on any of the four. HANDOFF treats them as "ready, never warmed" — true on warm-up state, but the oracle green-light implied by golden saga is unrecorded. Phase 6c UI walk should check whether the dashboard surfaces "never verified" vs "verified green" distinctly. P2 finding.

**DRIFT-4 (provisioning_jobs failures):** Three failed jobs since 2026-04-25 with `server_pair_id=null` (saga halted before pair materialization):
- `1f8e4ba8-294d-4676-a140-8ef576055ee2` 2026-04-24 — rolled_back — P18 partnerwithkroger.store (the original P18 saga; all 30 disabled accounts trace to a successor of this)
- `58f99fb4` 2026-04-25 22:03 — failed
- `4fe9574e` 2026-04-26 17:12 — failed
- `6565a01b` 2026-04-26 18:47 — failed

Phase 5 dives + Phase 1 session review will identify whether these are known-aborted attempts (e.g., for P19) or genuine new failures.

## Pre-flight gates summary

| Gate | Outcome |
|---|---|
| Saga golden tag sha unchanged | ✓ `c1cc0bf...` |
| Saga isolation invariant test | ✓ green |
| All 23 gate-0 tests | ✓ green |
| Three-way HEAD alignment | ✓ all on `00b3260` |
| Supabase row counts vs HANDOFF | ✓ exact match |
| Working tree clean | ✓ |
| Branch correct | ⚠ on `claude/vibrant-golick-ce8da1` (worktree auto-name) instead of `audit/full-2026-04-29` (prompt's name); will rebrand at PR creation; non-blocking |

**No HARD HALT conditions tripped. Phase 0 is GREEN to proceed pending Phase 0.6 cert audit (running in background).**

---

## Master Cowork independent verification (2026-04-29 ack)

Master Cowork ran an independent ground-truth probe and confirmed every Phase 0 claim above:
- Saga sha unchanged at `c1cc0bf96f7aed54a5e74c0f5cf20cb693263de1`
- 23-gate count correct (Master noted the prompt's "16" was stale; matches)
- Three-way HEAD aligned at `00b3260…`
- Supabase counts match HANDOFF + this report exactly (14 / 318 / 119 / 18 PV rows / 30 P18-disabled / 12 P19-sending-domains / 0 P19-accounts)
- **Zero reviewer disagreement.**

## Two additional drift items surfaced by Master Cowork

**DRIFT-5 (test:gate0 omits two existing test files):** Two test files exist on this branch but are NOT chained into the `test:gate0` npm script:

1. `src/lib/provisioning/__tests__/saga-dry-run.test.ts`
2. `src/__tests__/saga-sending-domains-insert-contract.test.ts`

Phase 4.2.a/b will determine whether the omission is intentional (e.g., dry-run test is run via a different harness) or drift (e.g., a test was added to the codebase but never wired into the gate). P2 finding pending Phase 4 verdict.

**DRIFT-6 (Cowork prompt sha typo in appendix probe):** The Cowork-side continuation prompt has a saga-sha typo in its appendix verification probe — `7a` and `a7` are transposed somewhere relative to the canonical `c1cc0bf96f7aed54a5e74c0f5cf20cb693263de1`. Non-blocking for this audit (this side's probe used the canonical sha and matched). Phase 9 prompt-patch candidate.
