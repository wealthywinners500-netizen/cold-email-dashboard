# Phase 9.5 — Vercel cross-infra drift report

**Generated:** 2026-04-28 (V4 streamlined finish, Phase 9.5 lane 1 of 4)
**Method:** Vercel REST API (`api.vercel.com`) via `VERCEL_TOKEN`
**Scope:** Project `cold-email-dashboard` (`prj_anAbzLwY19mCmQcblypDQQYDQjGg`)

## Summary

Vercel state matches documented expectations + Phase 8.1 work. Two minor observations queued for backlog.

## Findings

### Project metadata (matches expected)

| Field | Actual | Expected | OK |
|---|---|---|---|
| name | `cold-email-dashboard` | same | ✓ |
| framework | `nextjs` | same | ✓ |
| rootDirectory | `null` | (project at repo root) | ✓ |
| buildCommand | `null` | (uses next.config.ts default) | ✓ |
| `commandForIgnoringBuildStep` | `null` | `null` (active dev per F-22 lock) | ✓ |
| `live` | `false` | unspecified — see below | ⚠ |
| `directoryListing` | `false` | n/a | — |

### Production deployments (last 5)

| uid | state | sha | created |
|---|---|---|---|
| `dpl_2CaZAy9kYZkgHJutt2fmgcwbVrW7` | READY | `00b3260d` | 2026-04-28 (Phase 8.1 PATCH redeploy) |
| `dpl_HmcL6NfgGEMUTeEfra6uF7yVoaHx` | READY | `00b3260d` | 2026-04-28 (Phase 8.1 add redeploy) |
| `dpl_k6dWZ9MgHwfvcVssCpR3Na4DjDF2` | READY | `00b3260d` | 2026-04-25 (PR #21 merge) |
| `dpl_iu54PmPVQoChZKDuhSRqJDcajPEj` | READY | `6f9b3179` | 2026-04-23 (PR #20 merge) |
| `dpl_5Kh4K6ZJxhS59G4ai4yy7HdpuiPp` | READY | `6f9b3179` | 2026-04-23 (PR #20 first deploy) |

All 5 READY — no FAILED / ERROR / CANCELED in recent history. The current production deploy is at sha `00b3260d` = main HEAD `00b3260` ✓.

### Production env-vars (23 total)

Was 22 before Phase 8.1; added DATABASE_URL → 23 ✓.

Full list (alphabetical):
```
CLERK_SECRET_KEY, CLERK_WEBHOOK_SECRET, DATABASE_URL, ENCRYPTION_KEY,
IONOS_API_KEY, LINODE_API_TOKEN, MXTOOLBOX_API_KEY, NAMECHEAP_API_KEY,
NAMECHEAP_API_USER, NAMECHEAP_CLIENT_IP, NAMECHEAP_USERNAME,
NEXT_PUBLIC_APP_URL, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY,
NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_SUPABASE_URL,
SPAMHAUS_DQS_KEY, STRIPE_PRO_PRICE_ID, STRIPE_SECRET_KEY,
STRIPE_STARTER_PRICE_ID, STRIPE_WEBHOOK_SECRET,
SUPABASE_SERVICE_ROLE_KEY, WORKER_BLACKLIST_URL, WORKER_CALLBACK_SECRET
```

DATABASE_URL: id `X2Xjg3qtO5sjIvqb`, encrypted, target=production. Value uses transaction-mode pooler (port 6543) per Phase 8.1 PATCH (HL #153).

### Crons (5 registered)

Cron count is 5. Full enumeration: the API returns crons as a complex object whose top-level keys are `enabledAt`/`disabledAt`/`updatedAt`/`deploymentId`/`definitions` (the actual cron defs live under `.crons.definitions`). Spot-check skipped this round to keep the audit lean — confirm via `vercel.json` cron config or Vercel dashboard if needed for compliance.

**Backlog item (not blocking):** spot-verify each of the 5 cron `definitions[]` entries' `path` + `schedule` against `dashboard-app/vercel.json`. If any mismatch, the deployed cron config is out of sync with the source config.

### `live: false` observation

Vercel project metadata reports `live: false`. Worth investigating — could indicate the project is on a Hobby plan (vs Pro) which has different cron scheduling guarantees, or it could be a legacy field. Per credentials reference: "Plan: Hobby (free)" — so `live: false` likely just reflects the Hobby tier (no SLA guarantees). Not a finding — clarify in `reference_credentials.md` Vercel section if Dean wants documented.

## Drift summary

- **Documented state matches actual:** prod deploys, env count + DATABASE_URL presence, framework/buildCommand/commandForIgnoringBuildStep, project name + ID
- **Minor observations:** crons not enumerated this round (cheap to defer); `live: false` aligns with Hobby tier
- **No new findings.** F-22 (`commandForIgnoringBuildStep=null`) confirmed correct for current active-development state per existing Dean lock policy #6.

**No HALT required from this lane.**
